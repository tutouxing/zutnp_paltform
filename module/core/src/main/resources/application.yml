server:
  port: 8848
  max-http-header-size: 102400
  tomcat:
    basedir: /temp/tomcat
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  application:
    name: core
  boot:
    admin:
      client:
        url: http://localhost:5000
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: zutnlp
    password: zutnlp_123
    url: jdbc:mysql://202.196.37.146:3306/zutnlp_cs_admin?useUnicode=true&characterEncoding=utf8 #服务器开发
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
  servlet:
    multipart:
      max-file-size: 50Mb
      max-request-size: 50Mb
      location: E:\images\upload


eureka:
  instance:
    hostname: ${spring.application.name}
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://127.0.0.1:18888/eureka/  #本地开发
management:
  endpoints:
    web:
      base-path: /
      exposure:
        include: 'hystrix.stream,health,status'