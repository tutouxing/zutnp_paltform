server:
  port: 19999
  max-http-header-size: 102400 #设置请求头
  connection-timeout: 1000s
spring:
  application:
    name: nlp
eureka:
  instance:
    hostname: ${spring.application.name}
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://127.0.0.1:18888/eureka/