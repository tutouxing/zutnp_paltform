server:
  port: 8898
eureka:
  instance:
    hostname: ${spring.application.name}
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://127.0.0.1:18888/eureka/
spring:
  application:
    name: zuul
zuul:
  routes:
    api:
      path: /core/**
      serviceId: core